name: dotfiles
root: ~/.dotfiles

windows:
  - editor:
      layout: main-vertical
      panes:
        - nvim .
        - git status
  - build:
      panes:
        -
  - logs:
      layout: even-horizontal
      panes:
        - tail -f /var/log/messages
        - htop

